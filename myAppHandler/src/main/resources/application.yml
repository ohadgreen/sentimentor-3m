server:
  port: 8081

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}

youtube:
  api:
    key: ${YOUTUBE_API_KEY}

app:
  aiWorker:
    path: ${AI_WORKER_PATH:http://localhost:8082/api/aiworker}